/*
 * Click nbfs://nbhost/SystemFileSystem/Templates/Licenses/license-default.txt to change this license
 * Click nbfs://nbhost/SystemFileSystem/Templates/Classes/Class.java to edit this template
 */
package a1s12;
/*
import javax.sql.rowset.JdbcRowSet;
import javax.sql.rowset.RowSetProvider; 
public class RowSet {
    public static void main(String[] args) throws Exception {
        Class.forName("com.mysql.cj.jdbc.Driver");
        JdbcRowSet rowSet = RowSetProvider.newFactory().createJdbcRowSet();
        rowSet.setUrl("jdbc:mysql://localhost:3306/ucsm");
        rowSet.setUsername("root");
        rowSet.setPassword("");
        rowSet.setCommand("select * from cursos");
        rowSet.execute();
    while (rowSet.next()) {
        // Generating cursor Moved event
        System.out.println("codigo: " + rowSet.getString(1));
        System.out.println("nombre curso: " + rowSet.getString(2));
        System.out.println("credito general: " + rowSet.getString(3));
        }
    }
}
*/
